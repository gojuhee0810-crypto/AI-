# 광고센터 어드민 디자인 시스템

> 광고주가 캠페인·광고그룹·소재(배너)를 생성하고 심사 요청하는 카카오페이 광고센터 어드민 화면의 디자인 기준입니다.
> 아래는 실제 어드민 화면 스크린샷(캠페인 만들기 / 광고그룹 만들기 / 소재 만들기)을 보고 정리한 내용입니다.
> 정확한 컬러 hex, 폰트, spacing 토큰은 Figma MCP 연동 후 추출값으로 교체 예정이며, 그 전까지는 스크린샷 기반 추정치입니다.

## 1. 전체 플로우 (3단계)

```
1. 캠페인 만들기
   광고 상품(Fit 배너 / Full Screen 배너) 선택 → 캠페인 이름, 카테고리 지정
        ↓ "등록하고 광고그룹 만들기"
2. 광고그룹 만들기
   타겟팅, 집행 기간/시간, 예산(그룹 예산 / 일 예산) 설정
        ↓ "등록하고 소재 만들기"
3. 소재 만들기
   카피(메인/서브/안내문구), 이미지, 랜딩URL, 심사 참고사항 입력 → 미리보기 확인
        ↓ "등록하고 심사 요청하기"
```

우리 서비스(`adcenter-register` 스킬)는 이 3단계 중 **3단계 "소재 만들기"** 폼을 API/자동화로 채우는 것이 핵심 타깃입니다. 1~2단계(캠페인/광고그룹)는 광고주가 이미 만들어둔 것을 전제로 하거나, 추후 자동화 범위를 논의합니다.

## 2. 화면별 필드 정의

### 2-1. 캠페인 만들기
| 필드 | 타입 | 필수 | 제약 |
|---|---|---|---|
| 광고 상품 | 라디오 (Fit 배너 / Full Screen 배너) | ✓ | 우측에 상품별 미리보기 표시 |
| 캠페인 이름 | 텍스트 | ✓ | 25자 |
| 카테고리 | 2단계 depth 드롭다운 | ✓ | 대분류(교육/교통/금융/레저스포츠/반려동물/뷰티미용/생활서비스/쇼핑/온라인쇼핑/숙박/여행 등) → 소분류 |

### 2-2. 광고그룹 만들기
| 필드 | 타입 | 필수 | 제약 |
|---|---|---|---|
| 광고그룹 이름 | 텍스트 | ✓ | 25자 |
| 타겟팅 | 체크형 선택 (맞춤타겟 / 데모그래픽, 둘 중 하나만) | - | 맞춤타겟은 "카카오페이 광고 담당자로부터 전달받은 맞춤타겟 번호" 입력 방식 (툴팁 안내) |
| 집행 기간 | 날짜 범위 (시작일 - 종료일) | ✓ | 캘린더 피커 |
| 집행 시간 | 라디오 (모든 시간 / 요일별 시간대 설정) | ✓ | |
| 광고그룹 예산 (VAT 제외) | 숫자 입력 + 원 단위 표시 | ✓ | 캠페인 잔여 예산 이하 |
| 일 예산 (VAT 제외) | 숫자 입력 | ✓ | 광고그룹 예산 이하 |

### 2-3. 소재 만들기 (Fit 배너 기준)
| 필드 | 타입 | 필수 | 제약 |
|---|---|---|---|
| 소재 이름 | 텍스트 | ✓ | 25자 |
| 메인 카피 | 텍스트 | ✓ | **14자** (공백 포함) |
| 서브 카피 | 텍스트 | - | **15자** (공백 포함) |
| 안내 문구 | 텍스트 | - | 60자 (심의필 등 법적 고지용) |
| 이미지 | 파일 업로드 | ✓ | PNG, 500KB 이하, **240×240px** |
| 랜딩URL | URL 텍스트 | ✓ | `https://` 형식 |
| 심사 참고사항 | 텍스트영역 | - | 500자 |

> ⚠️ 이 글자수/이미지 스펙은 `image-style-patterns.md`, `copy-patterns.md`, `kakaopay-banner-guide.md`에도 반영해야 합니다 — 메인 카피 14자는 매우 타이트하므로 카피 생성 스킬의 프롬프트 제약을 이 값으로 맞춰야 합니다. (반영 완료: kakaopay-banner-guide.md)

**Full Screen 배너 소재 폼**은 이번 스크린샷에 포함되지 않음 — TBD. 다만 미리보기상 구성은 헤드라인(예: "3% 돌려드려요!") + 서브카피(혜택 요약) + 기간 정보 + CTA 버튼("혜택 확인하기") 구조로 추정됩니다.

## 3. 컬러 (스크린샷 기반 추정 — Figma에서 확정 필요)

| 토큰(가칭) | 추정 값 | 용도 |
|---|---|---|
| `color-header-bg` | 거의 블랙 (#000~#111) | 최상단 헤더, 좌측 사이드바 |
| `color-primary-cta` | 카카오 옐로우 계열 (#FEE500 부근) | 핵심 CTA 버튼 ("등록하고 소재 만들기", "등록하고 심사 요청하기") |
| `color-page-bg` | 라이트 그레이 (#F5F5F6 부근) | 본문 배경 |
| `color-surface` | 화이트 | 카드/인풋 배경 |
| `color-required` | 레드 | 필수 항목 `*` 표시 |
| `color-text-placeholder` | 그레이 | 인풋 placeholder |

## 4. 타이포그래피 (추정)

| 스타일 | 용도 |
|---|---|
| 대제목 (Bold, 큰 사이즈) | "광고그룹 만들기", "소재 만들기" 페이지 타이틀 |
| 필드 라벨 (Bold, 중간 사이즈) | 각 입력 필드명 |
| 본문/입력값 (Regular) | 인풋 텍스트, 안내문 |
| 캡션 (Small, Gray) | 글자수 카운터(`0/25`), 파일 스펙 안내 |

## 5. 레이아웃

- 좌측: 다크 톤 사이드바 (계정 선택 드롭다운 + "광고 관리" / "광고계정 관리" 네비게이션)
- 우측: 단일 컬럼 폼 영역, 상단에 breadcrumb 성격의 태그(캠페인명/광고그룹명 배지)
- "소재 만들기" 화면만 2컬럼: 좌측 폼 + 우측 실시간 미리보기(카카오페이 앱 내 실제 노출 시뮬레이션)
- 하단 고정 버튼 그룹: 취소(회색) / 등록(회색, 보조) / 등록하고 다음 단계(옐로우, 주요 CTA)

## 6. 컴포넌트 목록

- Breadcrumb 배지 (캠페인/광고그룹 태그)
- 텍스트 인풋 + 글자수 카운터 (`0/25` 형식)
- 라디오 그룹 (광고 상품, 집행 시간)
- 체크형 선택 카드 + 정보 툴팁 (타겟팅)
- 날짜 범위 피커
- 2단계 depth 카테고리 드롭다운
- 파일 업로드 (스펙 캡션 포함: 형식/용량/픽셀 사이즈)
- 텍스트영역 + 글자수 카운터
- 실시간 미리보기 패널 (모바일 앱 목업)
- 버튼 그룹 (Secondary 취소 / Secondary 등록 / Primary 등록+다음)

---
**업데이트 방법**: Figma MCP 연결 후 실제 컬러 hex, 폰트, spacing 토큰으로 3·4번 섹션을 교체하고, Full Screen 배너 소재 폼 스펙(TBD)을 채웁니다.
