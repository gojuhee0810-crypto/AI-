# Camera

25° isometric, slight top-down view, orthographic-like, centered, consistent perspective across every asset.

# Consistency Rules

Every illustration must use identical perspective, identical corner radius, identical depth, identical outline weight, identical surface construction, identical color logic, identical proportions. Every asset should look like it belongs to the same fintech illustration library.
